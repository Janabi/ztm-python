# an information that stored in memory and named iq and bound to 190
# snake_case
# start with lowercase or underscore
# letters, numbers ,and underscores
# case sensitive
# Don't override keywords

user_iq = 190
user_IQ = 191
# (user_iq/4) -> this is expression that would produce a value
# below is an entire statement
user_age = user_iq/4

a = user_age

# constant -> this number is not meant to be changed
PI = 3.14

# dunder variables would should not touch them that they start with "__"

a, b, c = 1, 2, 3

print(a)

# augmented assignment operator
some_value = 5
some_value += 2 # a shorthand for a previous title (some_value + 2)

print(some_value)